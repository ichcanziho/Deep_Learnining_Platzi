model = "vehicle_fine_tuned"
paths = {
    "LABELMAP": f"core/models/{model}/label_map.pbtxt",
    "PIPELINE_CONFIG": f"core/models/{model}/pipeline.config",
    "CKPT": f"core/models/{model}/checkpoint"
}
